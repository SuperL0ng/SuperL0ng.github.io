/* MLB LIVE TRACKING V50 */
(() => {
  'use strict';

  const MLB_SCHEDULE='https://statsapi.mlb.com/api/v1/schedule';
  const MLB_FEED='https://statsapi.mlb.com/api/v1.1/game';
  const ALIASES={AZ:'ARI',ARI:'ARI',OAK:'ATH',ATH:'ATH',KCR:'KC',KC:'KC',CHW:'CWS',CWS:'CWS',SDP:'SD',SD:'SD',SFG:'SF',SF:'SF',TBR:'TB',TB:'TB',WSN:'WSH',WAS:'WSH',WSH:'WSH'};
  const feedCache=new Map();
  let refreshRunning=false;

  const css=`
  .liveRefreshStatus{margin:0 0 10px;padding:9px 10px;border-radius:8px;background:rgba(255,255,255,.55);font-size:11px;color:#596372}
  .liveRefreshStatus.good{color:#236122;font-weight:800}.liveRefreshStatus.bad{color:#8b3200;font-weight:800}
  .liveLeg{border-top:1px solid rgba(0,0,0,.09);padding:8px 0}.liveLegTop{display:flex;justify-content:space-between;gap:10px;align-items:flex-start}.liveLegLabel{font-weight:850;font-size:13px}.liveLegValue{font-weight:900;font-size:12px;text-align:right}.liveLegMeta{margin-top:3px;color:#687383;font-size:10px;line-height:1.35}.liveStatus{display:inline-block;margin-top:4px;padding:3px 6px;border-radius:5px;font-size:9px;font-weight:900;letter-spacing:.08em}.liveStatus.WIN{background:#bfe3bd;color:#154e18}.liveStatus.LOSS{background:#efc1bc;color:#7a1710}.liveStatus.LIVE{background:#f1dda5;color:#674500}.liveStatus.PENDING{background:#d7dde6;color:#4f5966}.liveStatus.VOID{background:#d7dde6;color:#4f5966}.liveStatus.UNAVAILABLE{background:#e0cbd9;color:#683451}.liveSummary{margin:8px 0 2px;font-size:11px;font-weight:850;color:#596372}
  .ticketOutcome{display:inline-block;margin:8px 0 2px;padding:5px 9px;border-radius:7px;font-size:10px;font-weight:900;letter-spacing:.08em}.ticketOutcome.WON{background:#bfe3bd;color:#154e18}.ticketOutcome.LOST{background:#efc1bc;color:#7a1710}.ticketOutcome.LIVE{background:#f1dda5;color:#674500}.ticketOutcome.PENDING{background:#d7dde6;color:#4f5966}.ticketOutcome.PUSH{background:#d7dde6;color:#4f5966}.ticketOutcome.SUSPENDED{background:#e0cbd9;color:#683451}.liveTicketCard.ticketWon{box-shadow:inset 0 0 0 2px rgba(56,139,63,.22)}.liveTicketCard.ticketLost{box-shadow:inset 0 0 0 2px rgba(173,55,43,.22)}
  .liveLegValue.valueWin{color:#65D26E}.liveLegValue.valueLoss{color:#FF5E6C}.liveLegValue.valuePush{color:#FACC15}.liveLegValue.valueSuspended{color:#F59E0B}.liveLegValue.valuePending{color:#9CA3AF}.liveLegValue.valueTie{color:#7AA7FF}.liveLegValue.valueAhead1{color:#86EFAC}.liveLegValue.valueAhead2{color:#65D26E}.liveLegValue.valueAhead3{color:#22C55E}.liveLegValue.valueBehind1{color:#FACC15}.liveLegValue.valueBehind2{color:#FF8A8A}.liveLegValue.valueBehind3{color:#FF4D5E}`;

  function addCss(){if(document.getElementById('mlbLiveCss'))return;const s=document.createElement('style');s.id='mlbLiveCss';s.textContent=css;document.head.appendChild(s)}
  function cleanText(v){return String(v??'').trim()}
  function norm(v){return cleanText(v).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]/g,'')}
  function dateDash(v){v=cleanText(v).replace(/[^0-9]/g,'').slice(0,8);return v.length===8?`${v.slice(0,4)}-${v.slice(4,6)}-${v.slice(6)}`:v}
  function normalizedTeamCode(v){const code=cleanText(v).toUpperCase();return ALIASES[code]||code}
  function gameParts(game){game=cleanText(game);if(game.includes('@')){const [away,home]=game.split('@');return{away:normalizedTeamCode(away),home:normalizedTeamCode(home)}}if(game.includes(' v ')){const [home,away]=game.split(' v ');return{away:normalizedTeamCode(away),home:normalizedTeamCode(home)}}return{away:'',home:''}}
  function scheduleTeamCode(t){return normalizedTeamCode(t?.team?.abbreviation||t?.abbreviation||'')}

  async function jsonFetch(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json()}
  async function findGamePk(date,game){const p=gameParts(game),url=`${MLB_SCHEDULE}?sportId=1&date=${encodeURIComponent(dateDash(date))}&hydrate=team,linescore`;const data=await jsonFetch(url);const games=(data.dates||[]).flatMap(d=>d.games||[]);const match=games.find(g=>scheduleTeamCode(g.teams?.away)===p.away&&scheduleTeamCode(g.teams?.home)===p.home);return match?.gamePk||null}
  async function loadFeed(date,game){const key=`${date}|${game}`;if(feedCache.has(key))return feedCache.get(key);const promise=(async()=>{const gamePk=await findGamePk(date,game);if(!gamePk)throw new Error('Game not found');return jsonFetch(`${MLB_FEED}/${gamePk}/feed/live`)})();feedCache.set(key,promise);try{return await promise}catch(e){feedCache.delete(key);throw e}}

  function teamSide(feed,code){const away=normalizedTeamCode(feed?.gameData?.teams?.away?.abbreviation),home=normalizedTeamCode(feed?.gameData?.teams?.home?.abbreviation);code=normalizedTeamCode(code);if(code===away)return'away';if(code===home)return'home';return''}
  function scores(feed){const l=feed?.liveData?.linescore||{};return{away:Number(l.teams?.away?.runs||0),home:Number(l.teams?.home?.runs||0)}}
  function sideScore(feed,team){const side=teamSide(feed,team),s=scores(feed);return side?s[side]:null}
  function oppScore(feed,team){const side=teamSide(feed,team),s=scores(feed);return side?(side==='away'?s.home:s.away):null}
  function isFinal(feed){const s=feed?.gameData?.status||{};return s.abstractGameState==='Final'||/final|game over|completed/i.test(cleanText(s.detailedState))}
  function hasPitchOrCompletedPlay(feed){
    const plays=feed?.liveData?.plays||{};
    const meaningful=play=>Boolean(play?.about?.isComplete||play?.result?.eventType||play?.playEvents?.some(e=>e?.isPitch||e?.details?.isInPlay));
    return (plays.allPlays||[]).some(meaningful)||meaningful(plays.currentPlay);
  }
  function isLive(feed){return !isFinal(feed)&&hasPitchOrCompletedPlay(feed)}
  function hasStarted(feed){return isFinal(feed)||hasPitchOrCompletedPlay(feed)}
  function scheduledTime(feed){const raw=feed?.gameData?.datetime?.dateTime;if(!raw)return'';const d=new Date(raw);if(Number.isNaN(d.getTime()))return'';return d.toLocaleTimeString([],{hour:'numeric',minute:'2-digit'})}
  function gameState(feed){const l=feed?.liveData?.linescore||{},s=feed?.gameData?.status||{},sc=scores(feed);if(isFinal(feed))return`Final · ${sc.away}-${sc.home}`;if(!hasStarted(feed))return scheduledTime(feed)||cleanText(s.detailedState)||'Scheduled';const inning=l.currentInningOrdinal||l.currentInning||'',half=l.inningState||'',outs=Number(l.outs||0);return`${half} ${inning} · ${outs} out${outs===1?'':'s'} · ${sc.away}-${sc.home}`}
  function inningRuns(feed,through=5){const innings=feed?.liveData?.linescore?.innings||[];let away=0,home=0;innings.filter(x=>Number(x.num)<=through).forEach(x=>{away+=Number(x.away?.runs||0);home+=Number(x.home?.runs||0)});return{away,home}}
  function f5Complete(feed){return isFinal(feed)||Number(feed?.liveData?.linescore?.currentInning||0)>5||Number(feed?.liveData?.linescore?.currentInning||0)===5&&/middle|end/i.test(cleanText(feed?.liveData?.linescore?.inningState))}

  function allPlayers(feed){const teams=feed?.liveData?.boxscore?.teams||{};return['away','home'].flatMap(side=>Object.values(teams[side]?.players||{}).map(p=>({...p,__side:side})))}
  function findPlayer(feed,name){const n=norm(name);if(!n)return null;const players=allPlayers(feed);return players.find(p=>norm(p.person?.fullName)===n)||players.find(p=>norm(p.person?.fullName).includes(n)||n.includes(norm(p.person?.fullName)))||null}
  function batting(feed,name){return findPlayer(feed,name)?.stats?.batting||null}
  function pitching(feed,name){return findPlayer(feed,name)?.stats?.pitching||null}
  function pitcherFinished(feed,name){const player=findPlayer(feed,name),stats=player?.stats?.pitching||{};const appeared=Number(stats.battersFaced||0)>0||Number(stats.outs||0)>0||cleanText(stats.inningsPitched)!=='';return Boolean(appeared&&hasStarted(feed)&&!player?.gameStatus?.isCurrentPitcher)}
  function playStats(feed,name){const wanted=norm(name),out={hits:0,runs:0,rbi:0};if(!wanted)return out;const same=person=>{const n=norm(person?.fullName||person?.fullNameLastFirst||'');return n===wanted||n.includes(wanted)||wanted.includes(n)};for(const play of feed?.liveData?.plays?.allPlays||[]){if(same(play?.matchup?.batter)){const event=cleanText(play?.result?.eventType).toLowerCase();if(['single','double','triple','home_run'].includes(event))out.hits+=1;out.rbi+=Number(play?.result?.rbi||0)}for(const runner of play?.runners||[]){if(same(runner?.details?.runner)&&cleanText(runner?.movement?.end).toLowerCase()==='score'&&!runner?.movement?.isOut)out.runs+=1}}return out}
  function hrrbiLive(feed,name,b){const p=playStats(feed,name);return Math.max(Number(b?.hits||0),p.hits)+Math.max(Number(b?.runs||0),p.runs)+Math.max(Number(b?.rbi||0),p.rbi)}
  function totalBases(b){if(!b)return null;if(Number.isFinite(Number(b.totalBases)))return Number(b.totalBases);return Number(b.hits||0)+Number(b.doubles||0)+2*Number(b.triples||0)+3*Number(b.homeRuns||0)}
  function outsFromIP(ip){const s=cleanText(ip);if(!s)return 0;const [whole,frac='0']=s.split('.');return Number(whole||0)*3+Number(frac||0)}

  function result(status,current,target,display,meta){return{status,current,target,display:display??(current==null?'—':String(current)),meta:meta||''}}
  function milestone(current,target,feed){if(current==null)return result('UNAVAILABLE',null,target,'—','Player stat unavailable');if(current>=target)return result('WIN',current,target,`${current} / ${target}`);if(isFinal(feed))return result('LOSS',current,target,`${current} / ${target}`);return result(hasStarted(feed)?'LIVE':'PENDING',current,target,`${current} / ${target}`)}
  function overUnder(current,target,over,complete,started){if(current==null)return result('UNAVAILABLE',null,target,'—');const display=`${current} / ${target}`;if(over&&current>target)return result('WIN',current,target,display);if(!over&&current>=target)return result('LOSS',current,target,display);if(complete)return result(over?'LOSS':'WIN',current,target,display);return result(started?'LIVE':'PENDING',current,target,display)}

  function evaluateMlbLeg(leg,feed){const t=leg.type,target=Number(leg.target),team=leg.team,player=leg.player;if(t==='void')return result('VOID',null,null,'VOID');if(t==='manual'){const current=Number(leg.current??0),manualTarget=Number(leg.target??1);return result(current>=manualTarget?'WIN':'PENDING',current,manualTarget,`${current} / ${manualTarget}`)}const started=hasStarted(feed),final=isFinal(feed),score=sideScore(feed,team),opp=oppScore(feed,team);if(t==='ml'){if(score==null)return result('UNAVAILABLE',null,null,'—');if(!started)return result('PENDING',score,null,`${score}-${opp}`);if(final)return result(score>opp?'WIN':score<opp?'LOSS':'VOID',score,null,`${score}-${opp}`);return result('LIVE',score,null,`${score}-${opp}`)}if(t==='spread'){if(score==null)return result('UNAVAILABLE',null,target,'—');const adjusted=score+target;if(!started)return result('PENDING',adjusted,opp,`${score}${target>=0?'+':''}${target} vs ${opp}`);if(final)return result(adjusted>opp?'WIN':adjusted<opp?'LOSS':'VOID',adjusted,opp,`${score}${target>=0?'+':''}${target} vs ${opp}`);return result('LIVE',adjusted,opp,`${score}${target>=0?'+':''}${target} vs ${opp}`)}if(t==='f5_ml'||t==='f5_spread'||t==='f5_total_over'||t==='f5_total_under'){const r=inningRuns(feed,5),side=teamSide(feed,team),mine=side?r[side]:null,theirs=side?(side==='away'?r.home:r.away):null,total=r.away+r.home,done=f5Complete(feed);if(t==='f5_ml'){if(mine==null)return result('UNAVAILABLE',null,null,'—');if(!started)return result('PENDING',mine,null,`${mine}-${theirs}`);if(done)return result(mine>theirs?'WIN':mine<theirs?'LOSS':'VOID',mine,null,`${mine}-${theirs}`);return result('LIVE',mine,null,`${mine}-${theirs}`)}if(t==='f5_spread'){if(mine==null)return result('UNAVAILABLE',null,target,'—');const adj=mine+target;if(!started)return result('PENDING',adj,theirs,`${mine}${target>=0?'+':''}${target} vs ${theirs}`);if(done)return result(adj>theirs?'WIN':adj<theirs?'LOSS':'VOID',adj,theirs,`${mine}${target>=0?'+':''}${target} vs ${theirs}`);return result('LIVE',adj,theirs,`${mine}${target>=0?'+':''}${target} vs ${theirs}`)}return overUnder(total,target,t==='f5_total_over',done,started)}if(t==='total_over'||t==='total_under')return overUnder(scores(feed).away+scores(feed).home,target,t==='total_over',final,started);if(t==='team_total_over'||t==='team_total_under')return overUnder(score,target,t==='team_total_over',final,started);const b=batting(feed,player),p=pitching(feed,player);const map={player_hits:b?.hits,player_total_bases:totalBases(b),player_runs:b?.runs,player_hr:b?.homeRuns,player_rbi:b?.rbi,player_walks:b?.baseOnBalls,player_stolen_bases:b?.stolenBases,player_hwsb:b?Number(b.hits||0)+Number(b.baseOnBalls||0)+Number(b.stolenBases||0):null,player_hrrbi:hrrbiLive(feed,player,b),pitcher_ks:p?.strikeOuts};if(Object.prototype.hasOwnProperty.call(map,t)){const cur=Number(map[t]??0);if(t==='pitcher_ks'&&cur<target&&pitcherFinished(feed,player))return result('LOSS',cur,target,`${cur} / ${target}`,'Pitcher removed');return milestone(cur,target,feed)}if(t==='pitcher_ks_under'){const cur=Number(p?.strikeOuts||0);if(cur>=target)return result('LOSS',cur,target,`${cur} / U${target}`);if(final||pitcherFinished(feed,player))return result('WIN',cur,target,`${cur} / U${target}`,pitcherFinished(feed,player)?'Pitcher removed':'');return result(started?'LIVE':'PENDING',cur,target,`${cur} / U${target}`)}if(t==='pitcher_outs_under'){const cur=outsFromIP(p?.inningsPitched);if(cur>=target)return result('LOSS',cur,target,`${cur} / U${target}`);if(final||pitcherFinished(feed,player))return result('WIN',cur,target,`${cur} / U${target}`,pitcherFinished(feed,player)?'Pitcher removed':'');return result(started?'LIVE':'PENDING',cur,target,`${cur} / U${target}`)}return result('UNAVAILABLE',null,target,'—','Unsupported MLB leg')}

  function ticketGameForLeg(ticket,leg){return leg.game||ticket.game||''}
  function ticketDateForLeg(ticket,leg){return leg.date||ticket.date||''}
  function effectiveLeague(ticket,leg){return cleanText(leg?.league||ticket?.league).toUpperCase()}
  function ticketHasMlbOrLegacyLeg(ticket){return(ticket.legs||[]).some(leg=>{const l=effectiveLeague(ticket,leg);return!l||l==='MLB'})}
  async function evaluateRecord(record){const ticket=record.ticket||{},evaluated=[];for(const leg of ticket.legs||[]){const l=effectiveLeague(ticket,leg);if(l&&l!=='MLB'){evaluated.push({...leg,__live:result('UNAVAILABLE',null,leg.target,'—','Live support not added yet')});continue}const game=ticketGameForLeg(ticket,leg),date=ticketDateForLeg(ticket,leg);if(!game||!date){evaluated.push({...leg,__live:result('UNAVAILABLE',null,leg.target,'—','Missing game or date')});continue}try{const feed=await loadFeed(date,game);evaluated.push({...leg,__feed:feed,__live:evaluateMlbLeg(leg,feed)})}catch(e){evaluated.push({...leg,__live:result('UNAVAILABLE',null,leg.target,'—',e.message||'Live data failed')})}}return{...record,__evaluated:evaluated}}
  function ticketState(legs){const states=legs.map(l=>cleanText(l.__live?.status).toUpperCase()).filter(Boolean);if(states.includes('LOSS'))return'LOST';if(states.includes('UNAVAILABLE'))return'SUSPENDED';if(states.length&&states.every(s=>s==='VOID'))return'PUSH';if(states.length&&states.every(s=>s==='WIN'||s==='VOID'))return'WON';if(states.includes('LIVE'))return'LIVE';return'PENDING'}
  function scoreValueClass(leg,x,feed){const status=cleanText(x?.status).toUpperCase();if(status==='WIN')return'valueWin';if(status==='LOSS')return'valueLoss';if(status==='VOID')return'valuePush';if(status==='UNAVAILABLE')return'valueSuspended';if(status==='PENDING'||status!=='LIVE')return'valuePending';const t=cleanText(leg?.type);let margin=null;if(t==='ml')margin=sideScore(feed,leg.team)-oppScore(feed,leg.team);else if(t==='spread')margin=(sideScore(feed,leg.team)+Number(leg.target||0))-oppScore(feed,leg.team);else if(t==='f5_ml'||t==='f5_spread'){const r=inningRuns(feed,5),side=teamSide(feed,leg.team),mine=side?r[side]:null,theirs=side?(side==='away'?r.home:r.away):null;if(mine!=null&&theirs!=null)margin=mine+Number(t==='f5_spread'?leg.target||0:0)-theirs}if(margin==null||!Number.isFinite(Number(margin)))return'valuePending';margin=Number(margin);if(margin===0)return'valueTie';const m=Math.abs(margin),scale=2;if(margin>0)return m<scale?'valueAhead1':m<scale*2?'valueAhead2':'valueAhead3';return m<scale?'valueBehind1':m<scale*2?'valueBehind2':'valueBehind3'}
  function renderLiveCard(record){const t=record.ticket||{},legs=record.__evaluated||[],counts={WIN:0,LOSS:0,LIVE:0,PENDING:0,VOID:0,UNAVAILABLE:0};legs.forEach(l=>counts[l.__live?.status]=(counts[l.__live?.status]||0)+1);const states=Object.entries(counts).filter(([,n])=>n).map(([k,n])=>`${n} ${k}`).join(' · ');const firstFeed=legs.find(l=>l.__feed)?.__feed;const outcome=ticketState(legs),cardClass=outcome==='WON'?' ticketWon':outcome==='LOST'?' ticketLost':'';const isParlay=cleanText(t.type).toLowerCase()==='parlay';const topMeta=isParlay?[t.date]:[t.game,t.date,firstFeed?gameState(firstFeed):(record.status||'active').toUpperCase()];return`<div class="liveTicketCard${cardClass}"><div class="ticketTop"><div><span class="bookBadge">${esc(record.sportsbook||'Other')}</span><span class="title">${esc(t.title||'Untitled')}</span></div><span class="badge">${esc((t.type||'').toUpperCase())}</span></div><div class="meta">${esc(topMeta.filter(Boolean).join(' · '))}</div><div class="ticketOutcome ${outcome}">TICKET ${outcome}</div><div class="liveSummary">${esc(states||'No legs')}</div>${legs.map(l=>{const x=l.__live||result('UNAVAILABLE',null,l.target,'—'),legFeed=l.__feed||null,valueClass=scoreValueClass(l,x,legFeed||firstFeed),game=cleanText(l.game||t.game),legMeta=isParlay?[game,legFeed?gameState(legFeed):x.meta,l.team,l.player]:[l.team,l.player,x.meta];return`<div class="liveLeg"><div class="liveLegTop"><div><div class="liveLegLabel">${esc(l.label||'Untitled')}</div><div class="liveLegMeta">${esc(legMeta.filter(Boolean).join(' · '))}</div><span class="liveStatus ${esc(x.status)}">${esc(x.status)}</span></div><div class="liveLegValue ${valueClass}">${esc(x.display)}</div></div></div>`}).join('')}</div>`}

  async function refreshStandaloneLive(){if(refreshRunning)return;const params=new URLSearchParams(location.hash.slice(1)),id=params.get('ticket'),active=params.get('view')==='active';if(!id&&!active)return;refreshRunning=true;addCss();const box=document.getElementById('standaloneView');if(!box){refreshRunning=false;return}const list=loadSavedTickets(),items=active?list.filter(x=>x.status!=='completed'):list.filter(x=>x.id===id);document.getElementById('liveRefreshStatus')?.remove();const status=document.createElement('div');status.id='liveRefreshStatus';status.className='liveRefreshStatus';status.textContent='Refreshing live MLB data…';box.querySelector('.standaloneTools')?.insertAdjacentElement('afterend',status);try{const evaluated=[];for(const r of items)evaluated.push(ticketHasMlbOrLegacyLeg(r.ticket||{})?await evaluateRecord(r):{...r,__evaluated:(r.ticket?.legs||[]).map(l=>({...l,__live:result('UNAVAILABLE',null,l.target,'—','Live support not added yet')}))});const grid=box.querySelector('.liveGrid');if(grid)grid.innerHTML=evaluated.length?evaluated.map(renderLiveCard).join(''):'<div class="emptyState">Ticket not found on this device.</div>';status.textContent=`Updated ${new Date().toLocaleTimeString([],{hour:'numeric',minute:'2-digit',second:'2-digit'})}`;status.className='liveRefreshStatus good'}catch(e){status.textContent=`Live refresh failed: ${e.message||e}`;status.className='liveRefreshStatus bad'}finally{refreshRunning=false}}
  function wireRefresh(){const box=document.getElementById('standaloneView');if(!box||!location.hash)return;const refreshBtn=[...box.querySelectorAll('button')].find(b=>/^refresh$/i.test(cleanText(b.textContent)));if(refreshBtn)refreshBtn.onclick=()=>{feedCache.clear();refreshStandaloneLive()};refreshStandaloneLive()}

  window.__parlayLiveRefresh=()=>{feedCache.clear();refreshStandaloneLive()};
  window.addEventListener('parlay:viewchange',()=>setTimeout(wireRefresh,0));
  if(document.readyState==='loading')window.addEventListener('load',()=>setTimeout(wireRefresh,0),{once:true});else setTimeout(wireRefresh,0);
})();